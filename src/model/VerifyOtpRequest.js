/**
 * SellPass.WebApi
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The VerifyOtpRequest model module.
 * @module model/VerifyOtpRequest
 * @version v1
 */
class VerifyOtpRequest {
    /**
     * Constructs a new <code>VerifyOtpRequest</code>.
     * @alias module:model/VerifyOtpRequest
     * @param email {String} 
     * @param otp {String} 
     * @param recaptcha {String} 
     */
    constructor(email, otp, recaptcha) { 
        
        VerifyOtpRequest.initialize(this, email, otp, recaptcha);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, email, otp, recaptcha) { 
        obj['email'] = email;
        obj['otp'] = otp;
        obj['recaptcha'] = recaptcha;
    }

    /**
     * Constructs a <code>VerifyOtpRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/VerifyOtpRequest} obj Optional instance to populate.
     * @return {module:model/VerifyOtpRequest} The populated <code>VerifyOtpRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new VerifyOtpRequest();

            if (data.hasOwnProperty('email')) {
                obj['email'] = ApiClient.convertToType(data['email'], 'String');
            }
            if (data.hasOwnProperty('otp')) {
                obj['otp'] = ApiClient.convertToType(data['otp'], 'String');
            }
            if (data.hasOwnProperty('recaptcha')) {
                obj['recaptcha'] = ApiClient.convertToType(data['recaptcha'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>VerifyOtpRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>VerifyOtpRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of VerifyOtpRequest.RequiredProperties) {
            if (!data[property]) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['email'] && !(typeof data['email'] === 'string' || data['email'] instanceof String)) {
            throw new Error("Expected the field `email` to be a primitive type in the JSON string but got " + data['email']);
        }
        // ensure the json data is a string
        if (data['otp'] && !(typeof data['otp'] === 'string' || data['otp'] instanceof String)) {
            throw new Error("Expected the field `otp` to be a primitive type in the JSON string but got " + data['otp']);
        }
        // ensure the json data is a string
        if (data['recaptcha'] && !(typeof data['recaptcha'] === 'string' || data['recaptcha'] instanceof String)) {
            throw new Error("Expected the field `recaptcha` to be a primitive type in the JSON string but got " + data['recaptcha']);
        }

        return true;
    }


}

VerifyOtpRequest.RequiredProperties = ["email", "otp", "recaptcha"];

/**
 * @member {String} email
 */
VerifyOtpRequest.prototype['email'] = undefined;

/**
 * @member {String} otp
 */
VerifyOtpRequest.prototype['otp'] = undefined;

/**
 * @member {String} recaptcha
 */
VerifyOtpRequest.prototype['recaptcha'] = undefined;






export default VerifyOtpRequest;

